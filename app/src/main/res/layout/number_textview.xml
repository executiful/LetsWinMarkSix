<?xml version="1.0" encoding="utf-8"?>

    <androidx.constraintlayout.widget.ConstraintLayout  xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:clickable="false"
        >
        <!--    <androidx.constraintlayout.utils.widget.ImageFilterButton-->
        <!--        android:layout_width="@android:dimen/app_icon_size"-->
        <!--        android:layout_height="@android:dimen/app_icon_size"-->
        <!--        android:scaleType="fitCenter"-->

        <!--        android:src="@drawable/number_background" />-->
    <TextView
        android:id="@+id/id_dummy"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:lines="2"
        android:text="1111"
        android:textSize="@dimen/ticket_numsize"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/id_background"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="2dp"
        android:layout_marginBottom="2dp"
        android:contentDescription="TODO"
        android:enabled="false"
        android:scaleType="fitCenter"
        android:src="@drawable/ticket_number"
        android:visibility="visible"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/id_number"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/ticket_num_margin"
        android:clickable="false"
        android:enabled="false"
        android:fontFamily="sans-serif-smallcaps"
        android:gravity="center"

        android:lines="1"
        android:text="18"
        android:textAlignment="center"
        android:textColor="@color/color_ticket"
        android:textScaleX=".6"
        android:textSize="@dimen/ticket_numsize"
        android:typeface="serif"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />
    </androidx.constraintlayout.widget.ConstraintLayout>